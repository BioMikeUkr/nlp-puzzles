# Fixtures

## input/
- **sample_module.py** — Text processing utilities (clean_text, tokenize, count_words, extract_emails, etc.)
- **sample_ml_pipeline.py** — Simple sklearn text classification pipeline (TF-IDF + LogisticRegression)
- **sample_fastapi_app.py** — FastAPI sentiment prediction API with /health, /predict, /predict/batch

## expected/
- **coverage_example.json** — Example pytest-cov JSON output for reference
